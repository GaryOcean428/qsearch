{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": [
        { "cmd": "npm ci" },
        { "cmd": "npm run build" }
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run preview",
    "variables": {
      "NODE_ENV": "production",
      "VITE_QSEARCH_API_BASE": ""
    }
  }
}
